namespace FileManager;

public class AutocompleteFunction
{
    public static void Autocomplete(string commandSoFar)
    {
        /*
         * Get first letters
         * Find commands that include those letters
         * If there is only one command that fits, automatically add
         * the rest of the command name
         * Else show somewhere all the commands that fit
         */
    }
}